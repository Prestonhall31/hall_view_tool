require "spec_helper"

describe HallViewTool do
  it "has a version number" do
    expect(HallViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
